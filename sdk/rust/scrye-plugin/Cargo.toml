[package]
name = "scrye-plugin"
version = "0.1.0"
edition = "2021"
description = "SDK for writing Scrye MUD-client plugins in Rust (scrye-wasm-abi v1)"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
crate-type = ["rlib"]
