# .NET
bin/
obj/
*.user
.vs/
# Rider
.idea/
# NuGet local
packages/
# OS
.DS_Store
Thumbs.db
# Build output
[Dd]ebug/
[Rr]elease/
artifacts/
# milestone patch files (superseded by direct folder writes)
*.patch
# scratch dir used to sync files to the cloud build (not part of the project)
_stage_tmp/
# delivery scratch (zips staged/committed by the cloud bridge)
_incoming/
_to_delete/
# ...and the zips themselves. Nine of these were committed by accident during the early
# milestones: each one is a snapshot of source files that are already in the repo, so they
# were pure duplication sitting at the top of the file listing.
_*_delivery.zip

# local build output (self-contained zips)
dist/

# Rust SDK build output
sdk/rust/target/

# dev reports written for the 3Scapes staff - handed over directly, not shipped
# with the client, and each one is a point-in-time snapshot rather than a doc
# that gets maintained
docs/Dev-Report-*.md
